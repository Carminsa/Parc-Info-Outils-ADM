

<% if (req.flash('error') == "true"){ %>
<div class="alert alert-danger">
    <p class="error"> Bien tenté Florent ... </p>
</div>
<% } %>

<table class="table">

    <thead>
    <tr>
        <th>#</th>
        <th class="col-md-2">Numéro Chèque</th>
        <th>Fait le </th>
        <th>A</th>
        <th>Montant</th>
        <th>Nom caution </th>
        <th>Prénom caution</th>
        <th>profil de</th>
        <th>Mail caution</th>
        <th>Adresse caution</th>
        <th>ville caution</th>
        <th>Numéro de la caution</th>
    </tr>
    </thead>

    <%  for(var i = 0 ; i < users.length ; i++) {%>
    <%  for(var x = 0 ; x < users[i].cheque.length ; x++) {%>
    <tr>
        <td><%= users[i].cheque[x].id %></td>
        <td><%= users[i].cheque[x].number %></td>
        <td><%= users[i].cheque[x].expiration %></td>
        <td><%= users[i].cheque[x].in %></td>
        <td><%= users[i].cheque[x].amount %> €</td>
        <td><%= users[i].cheque[x].lastname %></td>
        <td><%= users[i].firstname %></td>
        <td><%= users[i].lastname %></td>
        <td><%= users[i].mail %></td>
        <td><%= users[i].cheque[x].adress %></td>
        <td><%= users[i].cheque[x].town %></td>
        <td><%= users[i].cheque[x].phone %></td>
    </tr>
    <% } %>
    <% } %>
</table>
